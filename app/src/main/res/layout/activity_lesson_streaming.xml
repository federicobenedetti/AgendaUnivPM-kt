<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="lessonStreamingViewModel"
            type="com.federicobenedetti.agendaunivpm.ui.main.viewmodels.LessonStreamingViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        tools:context=".ui.main.activities.LessonStreamingActivity">

        <LinearLayout
            android:id="@+id/linear_layout_loading"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone">

            <include
                layout="@layout/layout_loading"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:id="@+id/login_loading"
                android:layout_gravity="center"
                style="@style/SpinKitView.Small"></include>

        </LinearLayout>

    </LinearLayout>
</layout>
